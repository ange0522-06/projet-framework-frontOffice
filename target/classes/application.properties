# Nom de l'application
spring.application.name=reservationVoiture

# Local development settings
server.port=9091

# PostgreSQL local
spring.datasource.url=jdbc:postgresql://localhost:5432/reservation_voiture
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate (local)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# External reservations API (returns JSON list of reservations with hotel name)
external.api.reservations.url=http://localhost:8080/reservation/api/reservation/list
external.api.reservations.token=6ba7b810-9dad-11d1-80b4-00c04fd430c8
#api/reservation/list
# JSP view resolver
spring.mvc.view.prefix=/WEB-INF/jsp/
spring.mvc.view.suffix=.jsp
